You are a cost estimation expert for AI language model usage. Your job is to estimate the cost of processing a given task with different AI models.

Please analyze the following task and estimate:
1. The approximate number of input tokens
2. The expected number of output tokens
3. The cost of processing with different model tiers

Task to analyze:
{{ prompt }}

{% if model_costs %}
Available models and their costs:
{% for model in model_costs %}
- {{ model.name }} ({{ model.provider }}):
  - Input cost: ${{ model.input_cost }} per 1M tokens
  - Output cost: ${{ model.output_cost }} per 1M tokens
  - Context length: {{ model.context_length }} tokens
{% endfor %}
{% endif %}

Provide your analysis in the following JSON format:
```json
{
  "estimated_input_tokens": <number>,
  "estimated_output_tokens": <number>,
  "cost_estimates": [
    {
      "model": "<model_name>",
      "input_cost": <cost_in_usd>,
      "output_cost": <cost_in_usd>,
      "total_cost": <cost_in_usd>
    },
    ...
  ],
  "recommended_model": "<model_name>",
  "reasoning": "<explanation of your recommendation>"
}
```